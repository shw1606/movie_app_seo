import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
ReactDOM.render(

    <App />,// 이걸 컴포넌트 라고 한다. 이런 App. 그냥 펑션같은거 that returns html. JSX라고 한다. 여기 두개의 컴포넌트를 넣을 수 없다.

  document.getElementById('root')
);
